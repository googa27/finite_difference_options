# Finite-difference formula bundle

`finite_difference_options.contracts.formula_bundle.finite_difference_formula_bundle()` emits `formula_bundle.v1` payloads for FD stencils, LCP residuals, boundary closures, and method metadata. Styling remains owned by `ui_and_artifacts`.
